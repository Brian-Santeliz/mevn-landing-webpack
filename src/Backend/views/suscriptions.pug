extends ./layout/index
block contenido
    include includes/navigator
    .container  
        .row
            .col-md-8.mx-auto.mt-5
                h3.text-center #{titulo}
                p.text-center.mb-3 #{parrafo}
                    if(newsletters)
                            table.table.table-striped
                                thead.background-table.text-center
                                    tr
                                    th(scope='col') #
                                    th(scope='col') Email
                                for newsletter of newsletters
                                    tbody.text-center
                                        tr
                                        th(scope='row') #{newsletter.id}
                                        td #{newsletter.email}
